# Sample Submission

This template targets `private_test` and contains IDs for audio modality only.

Required files:

- `typeb_oeq.jsonl`
- `typea_oeq.jsonl`
- `mcq.jsonl`
- `tfq.jsonl`

Each line is a JSON object with the required id field and a string `response`.
Zip the four JSONL files when uploading to Codabench.
